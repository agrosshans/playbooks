---


tasks:

- name: "find _history"
  find:
    paths: /home
    patterns: '*_history'
    recurse: yes
  register: find_history

- name: "list _history files"
  debug: var="{{ item.path }}"
  with_items: "{{ find_history.files }}


730983